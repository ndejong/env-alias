#!/bin/sh

if [ -z ${1+x} ] || [ -z ${2+x} ]; then
  echo "Usage: env-alias <alias> [-d] <config.yml>"
  exit 1
fi

alias_name="${1}"
generator_bin="env-alias-generator"
shift

echo "alias \"${alias_name}\"=\"source <(${generator_bin} ${@})\""
